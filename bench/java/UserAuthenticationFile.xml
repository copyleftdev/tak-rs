<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<!--
    Empty user store. The bench uses anonymous TCP CoT inputs (no auth),
    so this file just has to be syntactically valid XML matching the
    upstream schema; the messaging service loads it at boot but never
    consults it.
-->
<UserAuthenticationFile xmlns="http://bbn.com/marti/xml/bindings"/>
