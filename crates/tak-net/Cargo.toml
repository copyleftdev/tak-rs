[package]
name         = "tak-net"
description  = "Network listeners (TCP / TLS / UDP / multicast / QUIC) for tak-rs."
version      .workspace = true
edition      .workspace = true
rust-version .workspace = true
license      .workspace = true
authors      .workspace = true
repository   .workspace = true

# Stub: only port constants today. Deps come back when listeners land (M1).
[dependencies]

[lints]
workspace = true
